# Notes

- deterministic by default: canonical entry order, DOS-epoch timestamps, UTF-8 names
- `--deterministic` additionally pins the pure-TS deflate encoder
- `--method store` keeps bytes verbatim (handy for tamper demos)
