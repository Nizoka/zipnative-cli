zipnative-cli sample input

This small text tree is archived by the samples under samples/create/.
Every byte here is committed, so archives built with --deterministic hash
the same on every machine.
